#include <iostream>
#include <vector>

using namespace std;

class Solution {
public:
	// pick the event that ends as early as possible
	// after picking remove all of the class Solution:
    def maxEvents(self, events: List[List[int]]) -> int:
    int maxEvents(vector<vector<int>>& events) {

        	

		return 0;
    }
};
